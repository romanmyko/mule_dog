http:
  host: "0.0.0.0"
  port: "8081"
  path: "api/*"
  base: ""

request_spaceX:
      protocol: "HTTPS"
      host: "api.spacexdata.com"
      port: "443"
      base: "/v4"

cores:
  endpoint: "/cores"
  
capsules:
  endpoint: "/capsules"
  
launchpads:
  endpoint: "/launchpads"

launches:
  endpoint: "/launches"

payloads:
  endpoint: "/payloads"

rockets:
  endpoint: "/rockets"

test:
  path: "/api"
  